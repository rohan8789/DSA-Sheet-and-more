void multiply(int A[][100], int B[][100], int c[][100], int n)
{
      //add code here.
      for(int i=0; i<n; i++)
      {
          for(int j=0; j<n; j++)
          {
              c[i][j]=0;
              for(int k=0; k<n; k++)
              {
                  c[i][j]=c[i][j]+A[i][k]*B[k][j];
              }
          }
      }
}
