class Solution {
  public:
    bool searchKey(int n, struct Node* head, int key) {
        Node* tail=head;
        while(tail!=NULL){
            if(tail->data==key){
                return true;
            }
            tail=tail->next;
        }
        return false;
    }
};
